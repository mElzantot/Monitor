
@model List<SubTask>

@for (int i = 0; i < Model.Count(); i++)
{
    <tr>
        <td>@Model[i].Name</td>
        <td>@Model[i].Description</td>
        <td>@Model[i].Priority</td>
        <td>@Model[i].Progress</td>
    </tr>
}
